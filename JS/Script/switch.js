var parametro = parseInt(prompt('insira um numero'))

        switch (parametro) {
            case 1:
                document.write('resultado')
                break;
            // é necessario q seja === (identico)
            case 2:
                document.write('resultado2')
                break;
            default:
                document.write('resultado3')
                break;
        }